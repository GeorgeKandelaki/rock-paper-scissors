import styled from "styled-components";

const StyledRipple = styled.div`
    position: absolute;
`;

function Ripple({ size }) {
    return <StyledRipple>winner</StyledRipple>;
}

export default Ripple;
